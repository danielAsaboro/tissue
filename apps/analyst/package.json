{
  "name": "@tissue/analyst",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "TISSUE analyst layer — read-only narration over the hash-chained ledger. Never decides.",
  "scripts": {
    "materialize": "tsx src/materializeCli.ts",
    "mcp": "tsx src/mcpCli.ts",
    "serve": "tsx src/serverCli.ts",
    "test": "vitest",
    "test:run": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@tissue/shared": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.0.4",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.13.4",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
